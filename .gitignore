# Chrome writes this when loading the extension unpacked
_metadata/

# Local Claude Code settings
.claude/settings.local.json

.DS_Store

# Built release zips — attach to a GitHub Release instead
dist/
